<div class="jumbotron">
  <h1>Handcrafted items from <br> 
    around the world</h1>
  <h2>Discover one-of-a-kind items</h2>
</div>

<div class='center'>
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3">

        <div class="thumbnail">
          <!-- Instead of having a word as a link, I want the word to be an image, it shoudl route to the listing url -->
          <%= link_to image_tag(listing.image.url), listing %>
          
          <div class="caption">
            <h3><%= listing.name %></h3>
            <p> <%= number_to_currency listing.price %></p>
            <!-- for the given listing, take the user and pull out the name of user -->
            <p><%= "Sold by #{listing.user.name}" %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<br>

<% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path, :class => "btn btn-link" %>
<% end %>